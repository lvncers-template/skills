---
name: test-run
description: アプリの品質とデプロイ可否を確認するため、型チェック・リント・テスト・ビルドを順に実行し、レポートとして報告する。コード修正は提案のみでその場では行わない。テストやビルドを一括で確認したいときに使う。
---

# テスト・ビルド一括実行

## 概要

アプリの品質とデプロイ可否を確認するため、型チェック・リント・テスト・ビルドを順に実行する。
**テスト実行とレポート作成に徹し、コード修正の提案はレポートに記載して報告する。**
以下は TypeScript プロジェクトを想定したコマンド例である。
プロジェクトの `package.json` に合わせて読み替えること。

## 手順

### 1. 開発サーバー起動

```sh
bun run dev
```

満たすべき基準

- サーバーが起動し、指定ポートで待ち受けていること

### 2. コード品質チェック

型チェック・リント・フォーマットチェックを実行する。フォーマットの自動適用は、ユーザーから明示的に依頼された場合のみ行う。

```sh
# 型チェック（コンパイルは行わない）
bunx tsc --noEmit

# リント
bun run lint

# フォーマットチェック（自動適用は依頼時のみ bun run format）
bun run format:check
```

満たすべき基準

- `tsc --noEmit` で型エラーが 0 件であること
- `lint` が警告・エラーなしで完了すること
- `format:check` でフォーマット差分がないこと

### 3. テスト実行

```sh
# ユニットテスト
bun run test:unit

# 統合テスト
bun run test:integration

# E2E テスト（開発サーバー起動済みであること）
bun run test:e2e

# 一括実行（定義されている場合）
bun run test:all
```

満たすべき基準

- 実行した分について全件成功（exit code 0）であること
- スキップ・pending はレポートに明記すること

### 4. カバレッジ

```sh
bun run test:coverage
```

満たすべき基準

- プロジェクトで定義されたカバレッジ閾値を満たしていること

### 5. ビルド検証

```sh
bun run build
```

満たすべき基準

- `build` が警告・エラーなしで完了すること

### 6. テストレポートの作成と報告

- 実施した手順と各ステップの成否
- 失敗・スキップしたテストの名前と理由
- コード修正の提案がある場合は箇条書きで記載
